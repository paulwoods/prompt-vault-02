server:
  servlet:
    context-path: /promptvault

spring:
  profiles:
    active: localhost
  application:
    name: backend
  datasource:
    url: ${PV_SPRING_DATASOURCE_URL}
    username: ${PV_SPRING_DATASOURCE_USERNAME}
    password: ${PV_SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: prompt-vault
  sql:
    init:
      mode: never

  flyway:
    enabled: true

  mail:
    host: ${PV_MAIL_HOST:smtp.gmail.com}
    port: ${PV_MAIL_PORT:587}
    username: ${PV_MAIL_USERNAME:}
    password: ${PV_MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  health:
    mail:
      enabled: false

app:
  jwt:
    secret: ${PV_JWT_SECRET}
    expiration: 604800000 # 7 days in milliseconds
  mail:
    from: ${PV_MAIL_FROM:noreply@promptvault.app}
    base-url: ${PV_APP_BASE_URL:http://localhost:5173}
